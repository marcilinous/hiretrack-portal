{
  "name": "hiretrack-portal",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "lint": "eslint api/ js/ --ext .js",
    "lint:fix": "eslint api/ js/ --ext .js --fix",
    "format": "prettier --write api/ js/ migrations/",
    "format:check": "prettier --check api/ js/",
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui",
    "test:e2e:headed": "playwright test --headed"
  },
  "devDependencies": {
    "@playwright/test": "^1.44.0",
    "eslint": "^9.3.0",
    "prettier": "^3.2.5"
  },
  "engines": {
    "node": ">=18"
  }
}
